mutation createAdmin($input: UserInput!) {
  createUser(data: $input) {
    _id
    uid
    firstName
    lastName
    email
    emailVerified
    submittedApplication
    isActivated
    isActive
    accType
  }
}
